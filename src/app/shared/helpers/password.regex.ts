export const PASSWORD_REGEXP: RegExp = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)[a-zA-Z\d]{8,}$/;

export const PASSWORD_UPPERCASE_LETTER_REGEXP: RegExp = /[A-Z]/;
export const PASSWORD_NUMBER_REGEXP: RegExp = /\d/;
